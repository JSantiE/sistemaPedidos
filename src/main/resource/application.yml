server:
  port: 8080
    
spring:
  jpa:
    platform: postgress
    generate-ddl: true
    show-sql: false
    hibernate:
      ddl-auto: update  
  datasource:
    platform: mysql
    url: ${DATABASE_URL}
    username: ${USER}
    password: ${PASSWORD}
  